el usuario toma una foto de su comida y el sistema le dice cuántas calorías tiene. Primero, la inteligencia artificial analiza la imagen y reconoce qué alimentos hay y más o menos cuánto hay de cada uno. Luego, el servidor usa esa información para calcular las calorías y nutrientes usando datos confiables. Esto se hace así porque la IA puede equivocarse con números, pero el sistema puede hacer cálculos más precisos. En resumen: la IA identifica la comida y el sistema hace los cálculos para dar un resultado más exacto.
